{
  "name": "neon-stack",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "verify": "node scripts/run-verify.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^6.3.5"
  }
}
