{
  "name": "neon-stack",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "pack": "tsc --noEmit && node scripts/pack-html.mjs",
    "preview": "vite preview",
    "verify": "node scripts/run-verify.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^6.3.5",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
